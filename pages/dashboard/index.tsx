import type { NextPage } from "next";
import { AuthWrapper } from "../../features";

const Dashboard: NextPage = () => {
  return <AuthWrapper>{/* Idea list */}</AuthWrapper>;
};

export default Dashboard;
